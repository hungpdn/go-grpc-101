version: v1
plugins:
  # Generate standard Go structs
  - plugin: go
    out: pb
    opt: paths=source_relative
  # Generate Go gRPC interfaces
  - plugin: go-grpc
    out: pb
    opt: paths=source_relative
  # Add the Gateway plugin
  - plugin: grpc-gateway
    out: pb
    opt: paths=source_relative
